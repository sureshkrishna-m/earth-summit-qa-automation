{
  "name": "earth-summit-qa-automation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:ci": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.1.6",
    "@types/node": "^20.11.0"
  }
}